{
  "dependencies": {
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "dotenv": "^17.2.0",
    "sequelize-cli": "^6.6.3"
  },
  "scripts": {
    "migrate:up": "npx sequelize-cli db:migrate",
    "migrate:down": "npx sequelize-cli db:migrate:undo"
  }
}
